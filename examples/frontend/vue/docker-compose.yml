# ---------------------------------------------------------------------------
# Docker Compose configuration for the IAM Vue Example.
#
# Services:
#   app - The Vue SPA served by Nginx, connecting to the platform
#         Keycloak instance running on the devops_iam-network.
#
# Usage:
#   docker compose up --build
# ---------------------------------------------------------------------------

services:
  # ---------- Vue SPA ----------
  app:
    build: .
    container_name: iam-vue-app
    ports:
      - "5174:80"
    env_file:
      - .env.example
    networks:
      - iam-network

networks:
  iam-network:
    external: true
    name: devops_iam-network
